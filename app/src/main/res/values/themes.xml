<resources>
    <!-- NoActionBar: top bar is handled entirely by Compose TopAppBar -->
    <style name="Theme.DomainManager" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Splash screen theme (core-splashscreen compat) -->
    <style name="Theme.DomainManager.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_animated</item>
        <item name="windowSplashScreenAnimationDuration">820</item>
        <item name="postSplashScreenTheme">@style/Theme.DomainManager</item>
    </style>
</resources>
